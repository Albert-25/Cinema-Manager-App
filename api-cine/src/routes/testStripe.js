// const stripe = require('stripe')(REACT_APP_SRIPE_PRIVATE_KEY);
// const { Router } = require("express");
// const router = Router();

// // const product = await stripe.products.create({
// //   name: 'Gold Special',
// // });

// const getAll = async (req, res, next) => {
//     try {
//         const product = await stripe.products.create({
//             name: 'Gold Special',
//         });
//         res.json(product);
//     } catch (error) {
//         next(error);
//     }
// };


// router.get("/", getAll);

// module.exports = router;

